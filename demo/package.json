{
  "name": "demo",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "rm -rf build && mkdir build",
    "build": "node_modules/.bin/browserify src/index.js --debug | exorcist build/index.js.map > build/index.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^9.0.3"
  },
  "dependencies": {
    "react": "^0.12.2"
  }
}
